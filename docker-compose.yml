version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: harvesthub_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: harvesthub_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - mongodb
    restart: always
    volumes:
      - ./firebase-service-account.json:/app/firebase-service-account.json:ro

  scraper:
    build:
      context: ./scraper
      dockerfile: Dockerfile
    container_name: athanur_scraper
    env_file:
      - .env
    depends_on:
      - mongodb
    restart: always
    volumes:
      - ./firebase-service-account.json:/app/firebase-service-account.json:ro

volumes:
  mongodb_data:
